<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edushare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.idea.Ideapage"></activity>
        <activity android:name=".ui.news.news1" />
        <activity android:name=".ui.news.news2" />
        <activity android:name=".ui.news.news3" />
        <activity android:name=".ui.news.news4" />
        <activity android:name=".ui.news.NewspageActivity" />
        <activity android:name=".ui.categoryItem.ArticlePage" />
        <activity android:name=".ui.myPage.Question.QuestionActivity" />
        <activity android:name=".ui.categoryItem.CategoryAPage" />
        <activity android:name=".ui.categoryItem.CategoryBPage" />
        <activity android:name=".ui.bottomNav.WrittingActivity" />
        <activity android:name=".ui.categoryMain.CategoryActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>